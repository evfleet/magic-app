import styled from '../../../../styles/themed-styled';

import { ContainerBase, WrapperBase } from '../../../../styles/base';

const Wrapper = styled(WrapperBase)`
  grid-area: footer;
`;

const Container = styled(ContainerBase)``;

export { Container, Wrapper };

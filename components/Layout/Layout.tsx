import React from 'react';

interface LayoutProps {}

const Layout: React.SFC<LayoutProps> = (): React.ReactElement => {
  return (
    <div>
      <p>Content</p>
    </div>
  );
};

export default Layout;

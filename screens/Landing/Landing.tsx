import React from 'react';

import Layout from '../../components/Layout';

const Landing: React.SFC<{}> = (): React.ReactElement => {
  return (
    <Layout>
      <p>Hello World</p>
    </Layout>
  );
};

export default Landing;
